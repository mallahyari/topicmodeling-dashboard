# React dashboard for topic modeling app

This repo will contain the code for the dashboard created in React.

To install all the packages, go to `frontend` directory and type:

`npm install`

To run the app:

`npm start`
